<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>FinestSelf</title>
  <!-- Font Awesome -->
    <script
      src="https://kit.fontawesome.com/55bc525515.js"
      crossorigin="anonymous"
    ></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script type=text/javascript>
            $(function() {
              $('button#test').click(function(e) {
                e.preventDefault()
                $.ajax({
                  url:  '/aspirations/' + e.target.value,
                  type: 'DELETE',
                  success: function() {
                    console.log('success dawg')
                    location.href = window.location.pathname
                  },
                  error: function(error) {
                    console.log('errored bro')
                    console.log(error)
                  }
                })
                return false;
              });
            });

            $(function() {
              $('button#delete_action').click(function(e) {
                e.preventDefault()
                $.ajax({
                  url:  '/actions/' + e.target.value,
                  type: 'DELETE',
                  success: function() {
                    console.log('success dawg')
                    location.href = window.location.pathname
                  },
                  error: function(error) {
                    console.log('errored bro')
                    console.log(error)
                  }
                })
                return false;
              });
            });

            $(document).ready(function() {
              $(".card.hover").hover(
                function() {
                  $(this).addClass('shadow-lg').css({'cursor': 'pointer', 'color': '#484848'})
                }, function() {
                $(this).removeClass('shadow-lg');
              }
              )
              $(".card.hover.btn-outline-primary").hover(
              function() {
                $(this).addClass('shadow-lg').css({'cursor': 'pointer', 'color': 'white'})
              }, function() {
                $(this).removeClass('shadow-lg');
              }
            );
          });

    </script>
  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-giJF6kkoqNQ00vy+HMDP7azOuL0xtbfIcaT9wjKHr8RbDVddVHyTfAAsrekwKmP1" crossorigin="anonymous">
  <!-- Custom CSS -->
  <link rel="stylesheet" href="/static/style.css">
</head>
